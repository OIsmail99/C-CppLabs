int main()
{
    int num = 0;
    int sum = 0;

    while(sum <= 100){

    printf("Enter a number: \n");
    scanf("%d", &num);
    sum = sum + num;
    printf("sum is now %d\n", sum);
    }
    printf("Number has exceeded 100\n");

    return 0;
}
